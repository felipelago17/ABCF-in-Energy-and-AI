# Dependencies for the ABCF enforcement-monitoring scripts.
requests>=2.31
feedparser>=6.0
python-dateutil>=2.8
anthropic>=0.40
PyYAML>=6.0
# Zero-cost LLM providers for the daily monitor (github / groq use the openai SDK;
# gemini uses google-generativeai, installed only when LLM_PROVIDER=gemini).
openai>=1.40
